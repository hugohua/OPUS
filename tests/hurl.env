# Hurl 测试环境配置
# 使用方法: hurl --variables-file tests/hurl.env tests/*.hurl

# 基础 URL
BASE_URL=http://localhost:3000

# Cron Secret (与 vercel.json 中配置一致)
CRON_SECRET=test-cron-secret-2026

# 测试用户标识 (方案 B: TEST_ 标记)
TEST_USER_ID=test_user_hurl_001
TEST_USER_EMAIL=test_hurl@opus.dev

# TTS 测试
TEST_TTS_TEXT=TEST_Hello World
